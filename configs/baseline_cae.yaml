experiment_name: "baseline_cae_z_axis"

# UPDATED: Point to local Colab NVMe storage to eliminate I/O starvation
data_dir: "/content/local_spectra_data/processed_spectrograms"

# Keep checkpoints writing to Drive so you don't lose the model if the runtime dies
save_dir: "/content/drive/MyDrive/spectra-cq-data/checkpoints/classical"

seeds: [42, 100, 2026]
epochs: 15

batch_size: 32
learning_rate: 0.001
train_split: 0.8

latent_dim: 8